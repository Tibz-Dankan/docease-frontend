import React, { Fragment } from "react";
import { ChatLayout } from "../layouts/ChatLayout";

export const Chat: React.FC = () => {
  return (
    <Fragment>
      <div>
        <ChatLayout />
      </div>
    </Fragment>
  );
};
